@model IEnumerable<DiscountType>

<table class="table my-2 table-striped table-light">
    <tr>
        <th>Name</th>
        <th>Discount Rate</th>
        <th></th>
    </tr>
    @foreach (var i in Model)
    {
        <tr>
            <td> @i.Name </td>
            <td> @i.DiscountRate </td>
            <td class="text-center">
                <form asp-action="Delete" method="post">
                    <a asp-action="Edit" class="btn btn-sm btn-warning fa fa-pencil-alt" asp-route-DiscountTypeID="@i.DiscountTypeID">                        
                    </a>
                    <input type="hidden" name="DiscountTypeID" value="@i.DiscountTypeID" />
                    <button type="submit" class="btn btn-sm btn-primary fa fa-trash">                       
                    </button>
                </form>
            </td>
        </tr>
    }
</table>
<div class="text-center">
    <a asp-action="Create" class="btn btn-info">
        Add Discount Type
    </a>
</div>